---
description: Sync với GitHub + đọc handoff để tiếp tục việc đang dở (mở đầu phiên local).
---
1. Chạy: `git checkout master && git pull --ff-only origin master`. Nếu fail → DỪNG, báo diverge, hướng dẫn xử lý thủ công.
2. Đọc `docs/SESSION_HANDOFF.md` và `docs/curriculum/CURRICULUM_STATUS.md`.
3. Tóm tắt cho người dùng: Current state + Active task + Open decisions. Hỏi họ muốn tiếp việc gì.
