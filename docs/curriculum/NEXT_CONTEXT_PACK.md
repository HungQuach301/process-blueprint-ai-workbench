# NEXT CONTEXT PACK — paste this to start a new chat/session

You are helping me build "Process Blueprint AI Workbench" (Next.js/TypeScript, WSL
Ubuntu). The single source of truth is `docs/curriculum/CURRICULUM_V7_3.md` in my repo.
Chat is a workspace; curriculum changes only via a CCR commit (`docs/curriculum/CCR_LOG.md`).

Rules you must follow:
- Do NOT invent lessons, gates, sub-steps, or "-min/-lite" variants. The official
  spine-minimum scopes (Bài 7-min, 9-min, 17B-min, 22-mini) ARE the curriculum —
  follow them as written. (See `NO_EXTRA_INTERMEDIATE_STEPS_RULE.md`.)
- Do NOT promote a spine-minimum lesson to its full Expansion scope while in the spine.
- No agent commit/push/apply without my approval (enforced by git hooks).
- Banking data discipline: never run a cloud/delegate session on a branch with real
  bank data; mask before any cloud call.

Current state — do NOT restate it here (that caused two-sources-of-truth drift).
Read these instead, in order, and treat them as authoritative:
1. `docs/SESSION_HANDOFF.md` — current state + active task (the lean entry point).
2. `docs/curriculum/CURRICULUM_STATUS.md` — rolling 2-week plan + done log.
3. `docs/NEXT_BRIEF.md` — the concrete task in flight, if any.
4. The relevant lesson section in `docs/curriculum/CURRICULUM_V7_3.md`.

Then propose the smallest next slice with its evidence (eval report / ADR / PR).
